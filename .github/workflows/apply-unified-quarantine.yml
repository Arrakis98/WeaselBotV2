name: Unified quarantine validation

on:
  push:
    branches:
      - unified-quarantine-routing

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
